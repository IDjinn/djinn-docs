---
title: Built-in Types
---

import { Table } from '@/components/table';

## Boolean

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Alias' },
    { header: 'Size' },
  ]}
  rows={[
    {
      cells: [<code>bool</code>, <code>i1</code>, '1 byte'],
      details: <p>Represents a boolean value. <code>true</code> or <code>false</code>.</p>,
    },
  ]}
/>

## Integers

> `i(n)` and `u(n)` are valid for any bit width `n`. You can create integers of arbitrary size, e.g. `i7`, `u3`, `i256`.

### Signed

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Size' },
    { header: 'Range' },
  ]}
  rows={[
    { cells: [<code>i8</code>, '1 byte', <span title="-128 to 127">-128 to 127</span>] },
    { cells: [<code>i16</code>, '2 bytes', <span title="-32,768 to 32,767">-32K to 32K</span>] },
    { cells: [<code>i32</code>, '4 bytes', <span title="-2,147,483,648 to 2,147,483,647">-2B to 2B</span>] },
    { cells: [<code>i64</code>, '8 bytes', <span title="-9,223,372,036,854,775,808 to 9,223,372,036,854,775,807">-9.2E to 9.2E</span>] },
    { cells: [<code>i128</code>, '16 bytes', <span title="-170,141,183,460,469,231,731,687,303,715,884,105,728 to 170,141,183,460,469,231,731,687,303,715,884,105,727">-170U to 170U</span>] },
  ]}
/>

### Unsigned

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Size' },
    { header: 'Range' },
  ]}
  rows={[
    { cells: [<code>u8</code>, '1 byte', <span title="0 to 255">0 to 255</span>] },
    { cells: [<code>u16</code>, '2 bytes', <span title="0 to 65,535">0 to 65K</span>] },
    { cells: [<code>u32</code>, '4 bytes', <span title="0 to 4,294,967,295">0 to 4B</span>] },
    { cells: [<code>u64</code>, '8 bytes', <span title="0 to 18,446,744,073,709,551,615">0 to 18E</span>] },
    { cells: [<code>u128</code>, '16 bytes', <span title="0 to 340,282,366,920,938,463,463,374,607,431,768,211,455">0 to 340U</span>] },
  ]}
/>

### Floating Point

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Size' },
    { header: 'Precision' },
  ]}
  rows={[
    { cells: [<code>f32</code>, '4 bytes', 'Single precision (IEEE 754)'] },
    { cells: [<code>f64</code>, '8 bytes', 'Double precision (IEEE 754)'] },
  ]}
/>

### Primitive Constants

All numeric types expose `MAX_VALUE` and `MIN_VALUE` constants. Floating-point types also expose `MIN_POSITIVE` and `EPSILON`:

```djinn
i32 max = i32.MAX_VALUE;   // 0x7FFFFFFF
i32 min = i32.MIN_VALUE;   // 0x80000000
u64 umax = u64.MAX_VALUE;  // 0xFFFFFFFFFFFFFFFF

f64 eps = f64.EPSILON;          // 2.2204460492503131e-16
f32 minPos = f32.MIN_POSITIVE;  // 1.17549435e-38
```

<Table
  columns={[
    { header: 'Type', width: 'w-1/6' },
    { header: 'MAX_VALUE' },
    { header: 'MIN_VALUE' },
  ]}
  rows={[
    { cells: [<code>i8</code>, '127', '-128'] },
    { cells: [<code>i16</code>, '32767', '-32768'] },
    { cells: [<code>i32</code>, '0x7FFFFFFF', '0x80000000'] },
    { cells: [<code>i64</code>, '0x7FFFFFFFFFFFFFFF', '0x8000000000000000'] },
    { cells: [<code>u8</code>, '255', '0'] },
    { cells: [<code>u16</code>, '65535', '0'] },
    { cells: [<code>u32</code>, '0xFFFFFFFF', '0'] },
    { cells: [<code>u64</code>, '0xFFFFFFFFFFFFFFFF', '0'] },
  ]}
/>

<Table
  columns={[
    { header: 'Type', width: 'w-1/6' },
    { header: 'MAX_VALUE' },
    { header: 'MIN_VALUE' },
    { header: 'MIN_POSITIVE' },
    { header: 'EPSILON' },
  ]}
  rows={[
    { cells: [<code>f32</code>, '3.40282347e+38', '-3.40282347e+38', '1.17549435e-38', '1.19209290e-7'] },
    { cells: [<code>f64</code>, '1.7976931348623157e+308', '-1.7976931348623157e+308', '2.2250738585072014e-308', '2.2204460492503131e-16'] },
  ]}
/>

### Size Types

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Alias' },
    { header: 'Description' },
  ]}
  rows={[
    { cells: [<code>size</code>, <code>u32</code>, 'Platform size type'] },
    { cells: [<code>size_long</code>, <code>u64</code>, '64-bit size type'] },
    { cells: [<code>c_result</code>, <code>i32</code>, 'C interop result type'] },
  ]}
/>

## Void

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>void</code>, 'Represents the absence of a value'],
      details: <p>Used as a return type for functions that do not return a value.</p>,
    },
  ]}
/>

## Strings

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>str</code>, 'String slice (borrowed)'],
      details: <>
        <p>A reference to a sequence of characters. Contains a pointer to the data and a length.</p>
        <pre><code>{`struct str {\n    i8* data;\n    size length;\n}`}</code></pre>
        <p><strong>Methods:</strong></p>
        <ul>
          <li><code>is_empty() -&gt; bool</code> — returns <code>true</code> if length is 0</li>
          <li><code>ptr() -&gt; i8*</code> — returns raw pointer to the data</li>
          <li><code>hash() -&gt; u64</code> — returns a hash of the string content</li>
          <li><code>slice(size start, size length) -&gt; str</code> — returns a sub-slice of the string</li>
          <li><code>substring(size start, size length) -&gt; string</code> — returns a new heap-allocated substring</li>
          <li><code>concat(string left, string right) -&gt; string</code> — concatenates two strings</li>
        </ul>
        <p><strong>Operators:</strong></p>
        <ul>
          <li><code>==</code> — compares two <code>str</code> values by content</li>
          <li><code>+</code> — concatenates two strings into a new <code>string</code></li>
        </ul>
      </>,
    },
    {
      cells: [<code>string</code>, 'Owned string (heap-allocated)'],
      details: <>
        <p>A growable, heap-allocated string. Contains a pointer, length, and capacity.</p>
        <pre><code>{`struct string {\n    mut i8* data;\n    size length;\n    size capacity;\n}`}</code></pre>
        <p><strong>Methods:</strong></p>
        <ul>
          <li><code>is_empty() -&gt; bool</code> — returns <code>true</code> if length is 0</li>
          <li><code>ptr() -&gt; i8*</code> — returns raw pointer to the data</li>
          <li><code>length() -&gt; i32</code> — returns the string length</li>
          <li><code>capacity() -&gt; i32</code> — returns the allocated capacity</li>
          <li><code>hash() -&gt; u64</code> — returns a hash of the string content</li>
          <li><code>slice(size start, size length) -&gt; str</code> — returns a sub-slice of the string</li>
          <li><code>substring(size start, size length) -&gt; string</code> — returns a new heap-allocated substring</li>
          <li><code>concat(string left, string right) -&gt; string</code> — concatenates two strings</li>
        </ul>
        <p><strong>Operators:</strong></p>
        <ul>
          <li><code>==</code> — compares two <code>string</code> values by content</li>
          <li><code>+</code> — concatenates two strings into a new <code>string</code></li>
        </ul>
      </>,
    },
  ]}
/>

## Arrays

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>{'arr<T>'}</code>, 'Array slice'],
      details: <>
        <p>A reference to a contiguous sequence of elements of type <code>T</code>. Contains a pointer and a length. Requires <code>T : Hashable</code>.</p>
        <pre><code>{`struct arr<T> : Hashable\nwhere T : Hashable {\n    T* data;\n    size length;\n}`}</code></pre>
        <p><strong>Methods:</strong></p>
        <ul>
          <li><code>is_empty() -&gt; bool</code> — returns <code>true</code> if length is 0</li>
          <li><code>ptr() -&gt; i8*</code> — returns raw pointer to the data</li>
          <li><code>hash() -&gt; u64</code> — computes a hash over all elements</li>
        </ul>
        <p><strong>Operators:</strong></p>
        <ul>
          <li><code>==</code> — compares two arrays by hash</li>
        </ul>
      </>,
    },
  ]}
/>

## Pointers

<Table
  columns={[
    { header: 'Type', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>T*</code>, 'Pointer to T'],
      details: <p>A raw pointer to a value of type <code>T</code>. Holds the memory address where the value is stored.</p>,
    },
  ]}
/>

## Reflection

### TypeInfo

```djinn
struct TypeInfo {
    i32 id;
    i32 size;
    i8* name;
    u8 kind;
}
```

Runtime type metadata available via the `typeof` intrinsic. The `kind` field encodes the category: `0` = integer, `1` = float, `2` = pointer.

### object

```djinn
struct object : Hashable {
    TypeInfo* type;
    void* data;
}
```

A type-erased value with runtime type information. Used internally by variadic functions to inspect argument types at runtime.

## Enums

### optional{'<T>'}

<Table
  columns={[
    { header: 'Variant', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>Empty()</code>, 'No value present'],
    },
    {
      cells: [<code>Value(T)</code>, 'Contains a value of type T'],
    },
  ]}
/>

```djinn
enum optional<T> {
    Empty(),
    Value(T)
}
```

### result{'<T, E>'}

<Table
  columns={[
    { header: 'Variant', width: 'w-1/4' },
    { header: 'Description' },
  ]}
  rows={[
    {
      cells: [<code>Ok(T)</code>, 'Success, contains the result value'],
    },
    {
      cells: [<code>Error(E)</code>, 'Failure, contains the error value'],
    },
  ]}
/>

```djinn
enum result<T, E> {
    Ok(T),
    Error(E)
}
```
